#include "main.h"

/**
 * main - Entry point
 *
 * Description: Prints a string using _puts function.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    char *str;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    _puts(str);
    return (0);
}
