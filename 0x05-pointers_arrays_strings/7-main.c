#include "main.h"

/**
 * main - Entry point
 *
 * Description: Prints the second half of a string.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    char *str;

    str = "0123456789";
    puts_half(str);
    return (0);
}
